
## 🚀 每次更新上傳的標準三指令

請在 VS Code 的終端機（Terminal）依序輸入以下指令：

### 步驟一：暫存所有變更

```bash
git add .

```

> **說明：** 那個 `.` 代表「當前目錄下的所有檔案」。這行指令會把所有新增加、修改、或刪除的檔案，全部打包放進準備區（Stage）。

### 步驟二：提交並記錄說明

```bash
git commit -m "這裡輸入你的修改紀錄說明"

```

> **說明：** ` -m` 後面的雙引號內，可以寫你這次改了什麼（例如：`git commit -m "fix: update gitignore and exclude js folder"`）。這就像是存檔點的標題。

### 步驟三：推送到 GitHub

```bash
git push

```

> **說明：** 因為你之前已經綁定過分支，通常直接打 `git push`，Git 就會自動把進度推送到 GitHub 上了。

---

## 🔍 常見的小狀況（以防萬一）

如果你輸入 `git push` 時，系統跳出提示，叫你必須指定分支（Branch），那請改輸入這行標準指令：

```bash
git push -u origin main

```

*(註：如果你的預設分支名稱比較舊，叫做 master，就把 `main` 改成 `master`)*

你剛剛調整完 `.gitignore` 之後，剛好可以用這三個指令來測試看看喔！上傳過程中有遇到任何紅字報錯嗎？
